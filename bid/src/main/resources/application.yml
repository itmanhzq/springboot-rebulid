# common configuration
server:
  port: 7112
  compression:
    enabled: true

spring:
  profiles:
    active: dev

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: com/fenlibao/bid/mapper/**/*.xml
